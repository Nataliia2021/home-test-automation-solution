{
  "name": "home-test-automation-solution",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean:playwright": "rm -rf playwright-report test-results",
    "test": "npx playwright test --reporter=html",
    "test:headed": "playwright test --headed",
    "docker:build": "docker build -t playwright-tests .",
    "docker:test": "docker run --rm -e BASE_URL=http://host.docker.internal:3100 my-playwright-tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.1.0",
    "dotenv": "^17.2.1",
    "typescript": "^5.9.2"
  }
}
